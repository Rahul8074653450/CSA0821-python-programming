arr = list(map(int, input("Enter numbers: ").split()))
count = {}
for num in arr:
    if num in count:
        count[num] += 1
    else:
        count[num] = 1
max_num = max(count, key=count.get)
print("Most Repeated Number:", max_num)
